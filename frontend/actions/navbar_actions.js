// import * as Util from '../util/navbar_api_util';

// export const RECEIVE_NAVBAR = "RECEIVE_NAVBAR";

// export const receiveNavbar = (payload) => ({
//   type: RECEIVE_NAVBAR,
//   payload,
// });

// export const fetchNavbar = () => (dispatch) =>
//   Util.fetchNavbar().then((payload) =>
//     dispatch(receiveNavbar(payload))
//   );